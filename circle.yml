machine:
  python:
    version: 3.4.3
  node:
    version: 6.0.0

test:
  override:
    - pip install -r requirements.txt
    - npm install
    - npm run prospector
    - npm run lint
    - npm run tests

deployment:
  production:
    branch: master
    heroku:
      appname: gallery-ideco
